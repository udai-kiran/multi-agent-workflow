> `[P1] Isolate formatting from unrelated and concurrent worktree changes` — Discovery examines the entire post-run worktree without recording a pre-run baseline, so pre-existing dirty/untracked files—and files being edited by another worker—are treated as files “the worker touched.” In the current worktree this selects numerous unrelated configuration files, logs, and reports. Formatters can therefore rewrite user-owned changes; the JSON formatter’s read-then-`mv` sequence can also overwrite a concurrent edit. [.claude/bin/pi-worker:181](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:181), [.claude/bin/pi-worker:186](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:186), [.claude/bin/format-file:82](/home/udai/personal/multi-agent-workflow/.claude/bin/format-file:82)

> `[P2] Include staged-only changes in discovery` — `git diff --name-only` only reports unstaged changes, while `git ls-files --others` only reports untracked files. A worker that stages a modified or newly added file makes it invisible to both commands, leaving it unformatted. This was reproduced with a staged new file omitted from `_changed_files`; the index needs separate handling such as `git diff --cached`. [.claude/bin/pi-worker:182](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:182)

> `[P2] Verify the final code after running auto-fixes` — Pi completes its tests and emits its final report before formatting starts, but this helper does more than whitespace formatting: `ruff check --fix`, ESLint `--fix`, and SQLFluff can rewrite code. Consequently, the installed report and recorded test results describe the pre-fix tree, while the final delivered code is untested and its extra changes are absent from the worker’s report. [.claude/bin/pi-worker:174](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:174), [.claude/bin/pi-worker:178](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:178), [.claude/bin/format-file:34](/home/udai/personal/multi-agent-workflow/.claude/bin/format-file:34), [.claude/bin/format-file:38](/home/udai/personal/multi-agent-workflow/.claude/bin/format-file:38)

> `[P2] Bound best-effort formatting before publishing the completed report` — Each formatter runs synchronously without a timeout before the captured report is validated or copied to its destination. In particular, `npx --yes` may perform network/package resolution. A stalled formatter can therefore prevent a completed Pi report from ever being installed; external termination also leaves only the temporary report governed by the cleanup trap. [.claude/bin/pi-worker:186](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:186), [.claude/bin/pi-worker:192](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:192), [.claude/bin/pi-worker:209](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:209), [.claude/bin/format-file:37](/home/udai/personal/multi-agent-workflow/.claude/bin/format-file:37)

> `[P2] Surface an unavailable worker formatter` — Pi-worker silently skips the entire post-run step when `format-file` is missing or lacks execute permission, while still publishing the report and preserving Pi’s success status. Conversely, the PostToolUse hook directly `exec`s it, so the same deployment problem makes every matched Write/Edit hook fail with status 126 or 127. The inconsistent behavior makes incomplete deployments hard to detect. [.claude/bin/pi-worker:179](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:179), [.claude/hooks/format-on-save.sh:12](/home/udai/personal/multi-agent-workflow/.claude/hooks/format-on-save.sh:12)

Test note: The extracted formatter body exactly matches the former inline implementation, so no formatter cases were lost. The hook’s JSON parsing remains compatible with its `Write|Edit` PostToolUse registration, and its resolved `hooks/../bin/format-file` path works in the current layout; the helper is currently executable (`775`). `bash -n`, `git diff --check`, ShellCheck, a hook-input smoke test, and a temporary Git discovery test were run successfully. Ordinary formatter failures do not overwrite `pi_status` or contaminate `report_file`; they are suppressed by `|| true`, although formatter stdout may reach pi-worker’s own stdout. No end-to-end Pi run or concurrent-writer test was performed.