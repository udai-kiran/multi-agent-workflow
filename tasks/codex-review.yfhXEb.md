> `[P1] Scope formatting to the current worker run` — The post-run query enumerates every unstaged tracked and untracked file currently dirty, with no pre-run baseline or ownership tracking. Consequently, a worker formats pre-existing user changes and changes made by other workers. Parallel worker execution is explicitly supported, so one worker can rewrite another worker’s file while it is still editing it, causing stale-edit failures or lost intermediate work. [.claude/bin/pi-worker:181](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:181), [.claude/agents/coordinator.md:54](/home/udai/personal/multi-agent-workflow/.claude/agents/coordinator.md:54)

> `[P2] Avoid Bash-4-only report-finalization code` — `pi-worker` uses `#!/usr/bin/env bash` with no minimum-version check, but `mapfile` is unavailable in Bash 3.2, including the system Bash on macOS. Under `set -e`, that command exits 127 after Pi has completed; the EXIT trap then deletes the captured report before it is installed, and the worker returns the formatter-infrastructure failure instead of `pi_status`. [.claude/bin/pi-worker:2](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:2), [.claude/bin/pi-worker:143](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:143), [.claude/bin/pi-worker:181](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:181)

> `[P2] Include staged files in post-run discovery` — `git diff --name-only` only reports worktree-versus-index changes, while `git ls-files --others` only reports untracked files. If the worker stages a new or modified file, it disappears from both queries and is never formatted. Use a query that includes index-versus-HEAD changes as well. [.claude/bin/pi-worker:182](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:182)

> `[P2] Do not apply unreported autofixes after the worker finishes` — Pi’s final report is captured before the formatting pass, but `format-file` performs `ruff check --fix` and `eslint --fix`, which may make code changes beyond whitespace. Those edits therefore cannot appear in the worker’s file/change list and occur after any tests the worker reported, leaving the delivered state untested and the report stale. [.claude/bin/pi-worker:174](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:174), [.claude/bin/pi-worker:186](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:186), [.claude/bin/format-file:33](/home/udai/personal/multi-agent-workflow/.claude/bin/format-file:33)

> `[P2] Preserve JSON file metadata during formatting` — The JSON branch writes through a default-mode `mktemp` file and moves it over the original. This replaces the inode and commonly changes permissions to `0600`; it also replaces symlinks rather than updating their targets. The new repository-wide worker pass exposes this inherited behavior to every discovered JSON file, including unrelated dirty files. [.claude/bin/format-file:82](/home/udai/personal/multi-agent-workflow/.claude/bin/format-file:82), [.claude/bin/format-file:84](/home/udai/personal/multi-agent-workflow/.claude/bin/format-file:84)

> `[P2] Handle a missing formatter consistently in the hook` — The worker silently skips formatting when `format-file` is absent or non-executable, but the PostToolUse hook unconditionally executes it. A missing file returns 127 and a non-executable file returns 126, turning every applicable Write/Edit hook invocation into a hook failure. This is a new deployment dependency introduced by the extraction; invoking it through Bash or checking it explicitly would preserve best-effort behavior. [.claude/hooks/format-on-save.sh:12](/home/udai/personal/multi-agent-workflow/.claude/hooks/format-on-save.sh:12), [.claude/bin/pi-worker:180](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:180)

> `[P3] Make Git filename handling NUL-safe` — Both Git commands and `sort` use newline-delimited output. Git quotes paths containing newlines and other special bytes, so the resulting string does not name the real file and the `-f` check silently skips it. Use Git’s `-z` output and NUL-aware collection/sorting. [.claude/bin/pi-worker:181](/home/udai/personal/multi-agent-workflow/.claude/bin/pi-worker:181)

Test note: the extracted formatter cases match the previous hook logic; the hook still reads Claude Code’s PostToolUse JSON, and its resolved path currently points to the executable `.claude/bin/format-file`. `bash -n` and `git diff --check` passed; ShellCheck found no additional correctness issues. Ordinary formatter failures are swallowed by the worker and do not overwrite `pi_status`, and formatter output cannot contaminate the already-captured report file. I did not run Pi or the real formatters; residual risk is concentrated in concurrent discovery and post-report mutations.