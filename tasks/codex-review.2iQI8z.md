> `[P1] Prevent optional probes from terminating the script` — `check_bin` returns 1 for a missing optional binary, but the new calls are unguarded under `set -e`. With every required binary available and `npx` absent, the script prints the optional warning and exits 1 before checking anything else or printing the success summary, contradicting the documented exit-code contract. The same happens silently for missing `codex` under `--quiet`. Add `|| true` or make optional failures return success. `scripts/check-binaries.sh:21`, `scripts/check-binaries.sh:45`, `scripts/check-binaries.sh:72`

> `[P1] Keep Codex required while review remains mandatory` — The description says Codex is “not currently needed,” but the coordinator requires a Codex review with “no skipping” and forbids declaring completion without it; `codex-reviewer` exits 127 when the CLI is absent. Once the optional-probe bug above is fixed, this checker would approve a host on which the documented workflow cannot finish. `.claude/agents/coordinator.md:93`, `.claude/agents/coordinator.md:144`, `.claude/agents/coordinator.md:310`, `scripts/check-binaries.sh:73`

> `[P2] Account for the active formatter dependency before making npx optional` — `npx` is not used only by `dsh-worker`: the enabled `Write|Edit` formatting hook invokes it unconditionally for JavaScript and TypeScript. Without `npx`, that hook exits nonzero under `set -e` and formatting fails. The description is therefore incomplete and “not currently needed” is false even if `dsh-worker` has been removed from automatic worker selection. `.claude/settings.json:21`, `.claude/hooks/format-on-save.sh:2`, `.claude/hooks/format-on-save.sh:33`, `scripts/check-binaries.sh:72`

> `[P3] Preserve the complete worker dependency inventory in the header` — The “Workers and the binaries they need” section now omits `dsh-worker`, `codex-worker`, and `codex-reviewer`, although all three wrappers remain shipped and the script itself still checks their dependencies. Marking those rows optional would be accurate; deleting them makes the header contradict the executable inventory. `scripts/check-binaries.sh:10`, `scripts/check-binaries.sh:72`, `.claude/bin/codex-reviewer:24`

> `[P3] Add the missing required Git install hint` — If the hints are intentionally limited to required tools, removing the `npx` and `codex` hints is consistent with the reclassification. However, `git` remains required and is the only required binary absent from the hint block, leaving that failure without remediation. This omission is pre-existing but remains in the touched block. `scripts/check-binaries.sh:56`, `scripts/check-binaries.sh:88`

No in-repository script sources this file or branches on its exit code. The only caller-like reference is manual post-copy guidance at `scripts/copy-agentic-files.sh:141`; therefore no separate cross-script exit-code regression was found.

Test note: `bash -n` and `git diff --check` passed. PATH-isolated probes reproduced exit 1 with all required binaries present and either `npx` or `codex` missing. No worker or hook integration tests were run, and the repository has no automated behavior test covering optional binaries.